---
- name: Bootstrap python (raw with Ansible become)
  hosts: raspis
  serial: 1
  gather_facts: false
  become: true

  tasks:
    - name: Install python3 and pip
      ansible.builtin.raw: "dnf -y install python3 python3-pip"
      changed_when: true
